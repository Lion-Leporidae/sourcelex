# 待办需求（下次继续）

## 1. Agent 聊天页面
- 新增 `/chat` 页面
- 左侧对话历史列表
- 主区域聊天气泡（用户 + AI）
- 流式输出（SSE 接入 `/agent/chat/stream`）
- 支持 Markdown 渲染

## 2. 调用链关系补全
- 检查当前 Tree-sitter 提取的调用关系是否完整
- 可能遗漏：Go 方法调用的 receiver 类型解析、接口方法、闭包内调用
- 需要对比实际代码验证提取准确性
- 检查符号表 import 解析是否正确工作

## 3. 搜索置信度（min_score）
- 语义搜索 API 加 `min_score` 参数
- 前端搜索增加阈值滑块
- 结果按相似度排序，低于阈值的灰显或隐藏

## 4. 星图调用链布局
- 中心节点放在画布中央（被选中的实体）
- 调用者和被调用者散射分布在四周
- 力导向布局但带径向约束（callers 在上方/左方，callees 在下方/右方）
- 调用链之间有线相连（带箭头）
- 类似"银河星图"效果：节点有大小和亮度区分
- 点击节点切换为新的中心，重新布局
